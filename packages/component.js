/**
 * 导出所有组件
 */

import Button from "./components/button";

// 导出所有组件，可以通过import UiDemo from '@4399/tdata-ui-demo';的方式来导入所有组件
export default [Button]

// 将Button导出，可以通过import { Button } from '@4399/tdata-ui-demo';的方式来导入Button组件
export { Button } 
